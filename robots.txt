# Build inputs, not pages. src/index.src.html is the bilingual source both
# published pages are generated from, so it duplicates their content almost
# exactly. It carries a canonical pointing at /, but it should not be crawled
# in the first place.
#
# Two things about the placement, both load-bearing:
#
#   1. These MUST sit inside this wildcard group. They previously appeared at
#      the end of the file, directly after `User-agent: cohere-ai`, which under
#      RFC 9309 grouping made them apply to cohere-ai alone — every other
#      crawler, Googlebot included, was free to index the duplicate source.
#      Googlebot has no group of its own here, so the wildcard group is where
#      the rule has to live to do the job it was written for.
#
#   2. They MUST come before `Allow: /`. Google resolves conflicts by longest
#      match, under which `Disallow: /src/` beats `Allow: /` regardless of
#      order — but other parsers (Python's stdlib robotparser among them) take
#      the first matching rule, and `Allow: /` matches every path. Ordering the
#      Disallows first makes the file mean the same thing under both readings
#      instead of depending on which one a given crawler implements.
User-agent: *
Disallow: /src/
Disallow: /build.py
Allow: /

# Two different jobs, two different answers.
#
# CITATION — welcome, and the reason /llms.txt exists. These agents fetch a page
# live, at the moment someone asks a question, so that an assistant can quote it
# and link back. Being quoted accurately in an answer is worth more to a studio
# this size than the traffic a link would have sent, and an ambiguous robots file
# is a reason for a crawler to skip a small domain. /llms.txt carries a
# condensed, factual summary of the services, the published research and its
# actual numbers, so a model citing this site has something better to work from
# than scraped marketing copy.
User-agent: OAI-SearchBot
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: ChatGPT-User
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: Claude-User
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: Claude-SearchBot
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: PerplexityBot
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: Perplexity-User
Disallow: /src/
Disallow: /build.py
Allow: /

# Search engines, stated explicitly for one reason: in production Cloudflare
# prepends its own `User-agent: *` group saying `Allow: /` ahead of this file.
# A parser that merges groups (Google's does) still honours the Disallows
# below; one that takes the first matching group (Python's stdlib robotparser
# among them) reads Cloudflare's and never reaches ours, leaving /src/ crawlable.
# Naming these agents removes the guess — they match a group of their own and
# never consult either wildcard.
User-agent: Googlebot
Disallow: /src/
Disallow: /build.py
Allow: /

User-agent: Bingbot
Disallow: /src/
Disallow: /build.py
Allow: /

# TRAINING — declined. These are bulk crawlers that build training corpora
# rather than answer a question in front of a reader, and the distinction is the
# whole point: this site wants to be cited, not absorbed.
#
# This section previously said `Allow: /` for all four, which was written before
# there was a training position at all and quietly conflated the two jobs above.
# Cloudflare now serves a managed block ahead of this file asserting
# `Content-Signal: search=yes,ai-train=no,use=reference` — a reservation of
# rights under Article 4 of EU Directive 2019/790 — and disallows exactly these
# agents. The file said the opposite of the header, which is the kind of
# disagreement that makes a robots file mean whatever a given parser decides.
#
# Google-Extended and Applebot-Extended are not crawlers at all; they are
# training opt-out tokens read by Gemini and Apple Intelligence. Googlebot and
# Applebot are untouched, so search indexing is unaffected by either line.
#
# If this stance is ever reversed, change it in BOTH places — here and in
# Cloudflare's AI Crawl Control -> Signals. Changing one alone recreates the
# contradiction this section was written to end.
User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: cohere-ai
Disallow: /

Sitemap: https://cindrasec.com/sitemap.xml

# Condensed, factual summary for language models (llmstxt.org convention).
# Not a sitemap directive — crawlers that do not know it will ignore this line.
LLMs: https://cindrasec.com/llms.txt
